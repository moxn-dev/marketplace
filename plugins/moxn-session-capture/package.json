{
  "name": "@moxn/session-capture-plugin",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "esbuild src/hook.ts src/sync-one.ts src/drain.ts src/setup.ts --bundle --platform=node --target=node18 --format=cjs --outdir=dist --out-extension:.js=.cjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "20.4.4",
    "esbuild": "^0.24.0",
    "typescript": "5.1.6"
  }
}
